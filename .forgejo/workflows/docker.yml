name: Build & Publish Docker Image

on:
  push:
    branches:
      - main
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: docker:cli
    # No DinD service needed — the runner injects DOCKER_HOST pointing to
    # the compose DinD sidecar via host-gateway. docker:cli just uses it.
    steps:
      - name: Checkout
        run: |
          apk add --no-cache git
          git clone ${{ github.server_url }}/${{ github.repository }} repo
          cd repo
          git checkout ${{ github.sha }}

      - name: Build and push
        run: |
          REF="${{ github.ref }}"
          case "$REF" in
            refs/tags/v*) TAG="${REF#refs/tags/v}" ;;
            *)            TAG="latest" ;;
          esac
          IMAGE=git.steltner.cloud/${{ github.repository }}
          echo "${{ secrets.FORGEJO_TOKEN }}" | \
            docker login git.steltner.cloud -u ${{ github.repository_owner }} --password-stdin
          docker build -t "${IMAGE}:${TAG}" repo/
          docker push "${IMAGE}:${TAG}"