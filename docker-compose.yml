services:
  bandcamp-dl:
    build: .
    ports:
      - "5000:5000"
    user: "1000:1000"
    volumes:
      - /home/tobias/Music:/downloads
    environment:
      - DOWNLOAD_DIR=/downloads
    restart: unless-stopped
